{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "ExecuteMsg",
  "oneOf": [
    {
      "description": "Pool UST to anchor yield in order to get automated ticket with earning",
      "type": "object",
      "required": [
        "pool"
      ],
      "properties": {
        "pool": {
          "type": "object"
        }
      },
      "additionalProperties": false
    },
    {
      "description": "UnPool tokens and get back UST, un-bonding period required!",
      "type": "object",
      "required": [
        "un_pool"
      ],
      "properties": {
        "un_pool": {
          "type": "object",
          "required": [
            "amount"
          ],
          "properties": {
            "amount": {
              "$ref": "#/definitions/Uint128"
            }
          }
        }
      },
      "additionalProperties": false
    },
    {
      "description": "Withdraw unPool tokens after un-bonding period succeed",
      "type": "object",
      "required": [
        "claim_un_pool"
      ],
      "properties": {
        "claim_un_pool": {
          "type": "object"
        }
      },
      "additionalProperties": false
    },
    {
      "description": "Redeem earning from anchor",
      "type": "object",
      "required": [
        "redeem_earning"
      ],
      "properties": {
        "redeem_earning": {
          "type": "object"
        }
      },
      "additionalProperties": false
    }
  ],
  "definitions": {
    "Uint128": {
      "description": "A thin wrapper around u128 that is using strings for JSON encoding/decoding, such that the full u128 range can be used for clients that convert JSON numbers to floats, like JavaScript and jq.\n\n# Examples\n\nUse `from` to create instances of this and `u128` to get the value out:\n\n``` # use cosmwasm_std::Uint128; let a = Uint128::from(123u128); assert_eq!(a.u128(), 123);\n\nlet b = Uint128::from(42u64); assert_eq!(b.u128(), 42);\n\nlet c = Uint128::from(70u32); assert_eq!(c.u128(), 70); ```",
      "type": "string"
    }
  }
}
